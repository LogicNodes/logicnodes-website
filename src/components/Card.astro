---
export interface Props { 
  tight?: boolean;
  class?: string;
}
const { tight = false, class: className = '' } = Astro.props;
---
<div class={`bg-white rounded-2xl shadow-xl ${tight ? 'p-6' : 'p-8 md:p-10'} ${className}`}>
  <slot />
</div> 