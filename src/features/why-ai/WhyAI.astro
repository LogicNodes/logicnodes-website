---
/*
  Why‑AI section refactor
  • Graphic only loads ≥ lg (1024 px) – hidden on phones
  • Text card now flows with normal vertical rhythm on small screens
*/
import WhyAIGraphic from './WhyAIGraphic.astro';
import Button from '@components/Button.astro';
---
<section class="bg-ln-light py-section px-gutter">
  <div class="mx-auto max-w-7xl relative">
    <div class="flex flex-col lg:flex-row items-start">
      <!-- ── TEXT CARD ────────────────────────────────────────────── -->
      <div class="relative z-10 w-full lg:w-3/5 mb-16 lg:mb-0 mt-16 sm:mt-24 lg:mt-48">
        <div class="bg-white rounded-2xl shadow-xl p-8 md:p-10">
          <span class="uppercase tracking-wider text-sm font-semibold text-ln-accent">
            EN TEKNOLOGISK TRANSFORMATION
          </span>
          <h2 class="mt-2 text-heading-xl font-extrabold leading-tight text-ln-dark">
            Hvorfor AI?
          </h2>
          <p class="mt-4 space-y-4 text-body text-ln-dark">
            Mange virksomheder ved, at AI rummer et stort potentiale – men få har et klart billede af, helt konkret hvordan.<br /><br />
            Computeren virkede også abstrakt i starten. I dag er den uundværlig i enhver virksomhed.<br /><br />
            AI er den næste teknologiske transformation – og første skridt er indsigt.<br /><br />
            Book en gratis samtale med en AI-ekspert fra LogicNodes. På under 30 minutter får I overblik over mulighederne og er klar til at tage det første skridt.
          </p>
          <div class="flex gap-4 mt-6">
            <!-- Outline first, filled second (matches the mockup) -->
            <Button href="#kontakt" variant="primary" className="flex-1">
              Kontakt os
            </Button>
          </div>
        </div>
      </div>

      <!-- ── GRAPHIC – desktop only ─────────────────────────────── -->
      <div class="hidden lg:block w-full lg:w-1/2 lg:absolute lg:right-0 lg:top-16">
        <WhyAIGraphic
          class="pointer-events-none w-full h-auto scale-110 lg:scale-125 max-w-full"
          aria-hidden="true"
        />
      </div>
    </div>

    <!-- Spacer for overlap – shorter on mobile -->
    <div class="h-16 sm:h-24 lg:h-[300px]"></div>
  </div>
</section>
