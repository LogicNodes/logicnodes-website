---
// src/lib/Icon.astro
export interface Props { name: string; class?: string }

const Svg = (await import(`../assets/icons/ui/${Astro.props.name}.svg?react`))
             .default;
const cls   = Astro.props.class ?? '';
---

<Svg className={cls} width="1em" height="1em" />
